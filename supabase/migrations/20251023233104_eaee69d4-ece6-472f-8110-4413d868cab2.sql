-- RETIRED LEGACY CRON MIGRATION.
-- The original revision embedded an environment-specific URL and JWT-like
-- value. It has already been applied historically and must not be replayed.
-- Current cron is governed by the separately reviewed live cron manifest.
SELECT 1;
